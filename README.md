# NeuralNetworkExperiment

A neural network created from scratch (no TensorFlow or PyTorch) in order to complete image recognition tasks. Initially trained with the MNIST dataset in order to test proper function (average performance of 95% in testing). The neural network was then tested using the SIIM ISIC dataset to determine whether a mole is benign or malignant (average performance of 83% in testing).
